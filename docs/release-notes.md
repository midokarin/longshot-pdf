Longshot PDF 0.2.4 fixes the broken logo on the Obsidian Community preview by using a direct image URL in the English and Chinese README files. The quick settings screenshot uses a direct URL as well. Plugin export behavior is unchanged.

The GitHub Actions release includes `main.js`, `manifest.json`, and `styles.css`, with build provenance attestations. Replace these three files to update an existing installation, then reload the plugin.

Longshot PDF 0.2.4 修复 Obsidian 社区预览页的 Logo 破图：中英文 README 改用可直接访问的图片地址，快速设置截图也做了相同调整。插件导出行为未变。

此版本由 GitHub Actions 构建，附带 `main.js`、`manifest.json`、`styles.css` 和构建来源证明。更新已有安装时替换这三个文件，再重新加载插件。
